<p style="color: green"><%= notice %></p>

<%= render @notice %>

<div>
  <%= link_to "Edit this notice", edit_notice_path(@notice) %> |
  <%= link_to "Back to notices", notices_path %>

  <%= button_to "Borrar", @notice, method: :delete %>
</div>
